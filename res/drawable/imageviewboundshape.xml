<?xml version="1.0" encoding="utf-8"?>
<!-- 定义矩形rectangle -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    
    <!-- 设置边框的大小和颜色 -->
    <stroke android:width="3dip" android:color="#39A1E8"/>
    
    <!-- 设置矩形内的颜色,此处为透明色 -->
    <solid android:color="@android:color/transparent"/>
    
    <corners 
        android:bottomLeftRadius="@dimen/btn_radius_common"
        android:bottomRightRadius="@dimen/btn_radius_common"
        android:topLeftRadius="@dimen/btn_radius_common"
        android:topRightRadius="@dimen/btn_radius_common"/>

</shape>
